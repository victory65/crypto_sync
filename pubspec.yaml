name: crypto_sync
description: "Crypto Sync - Master-Investor Real-Time Trade Mirroring System"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.2
  fl_chart: ^1.1.1
  go_router: ^17.1.0
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  flutter_animate: ^4.5.0
  web_socket_channel: ^3.0.1
  http: ^1.2.2
  crypto: ^3.0.5
  connectivity_plus: ^6.1.0
  intl: ^0.19.0
  local_auth: ^2.3.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
