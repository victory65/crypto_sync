name: crypto_sync
description: "Crypto Sync - Master-Slave Real-Time Trade Mirroring System"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.2
  fl_chart: ^1.1.1
  go_router: ^17.1.0
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  flutter_animate: ^4.5.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
